# 在linux系统中使用,用来检测某个终端程序输出了几行消息

使用前先要对hook.c 进行编译,生成hook.so

使用下边命令编译
``` bash
gcc -shared -fPIC hook.c -o hook.so -ldl
```

使用时修改run.py的第7,8行

``` python
OPENSH = "./open.sh"   #修改这里的检测程序路径
CMDCOUNT = 5           #要检测的命令行数量
```

当这个程序输出指定行数时,程序会向局域网发一个UDP广播,通知点击器,让所有点击头抬起

板子上使用出厂程序就好